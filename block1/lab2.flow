import material/material;

/*Написать функцию, считающую сумму целых чисел в массиве: [1, 2, 3] в 6. 
Использовать функции: fold.*/

main(){
    arr : [int] = [1,2,3];
    sum = fold(arr, 4, \x,y -> x+y);
    println(sum);
    quit(0);
}